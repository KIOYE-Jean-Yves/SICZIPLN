% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Uselful_functions.R
\name{grad_M_step}
\alias{grad_M_step}
\title{Gradient of the Variational M-Step Objective Function}
\usage{
grad_M_step(X, Y, O = O, paramsM, Omega, M, S, R, X_zero, lambda, epsilon)
}
\arguments{
\item{X}{Numeric matrix of covariates for the latent Gaussian component
(\eqn{n \times d}).}

\item{Y}{Matrix of observed count data (\eqn{n \times p}).}

\item{O}{Matrix of offsets on the log scale (\eqn{n \times p}).}

\item{paramsM}{Numeric vector containing stacked model parameters:
regression coefficients for the latent Gaussian model (\code{B}) followed
by coefficients for the zero-inflation model (\code{B_zero}).}

\item{Omega}{Precision matrix of the latent Gaussian distribution
(\eqn{\Omega = \Sigma^{-1}}).}

\item{M}{Matrix of variational means (\eqn{n \times p}).}

\item{S}{Matrix of variational standard deviations (\eqn{n \times p}).}

\item{R}{Matrix of posterior probabilities of structural zeros (\eqn{n \times p}).}

\item{X_zero}{Design matrix for the zero-inflation component.}

\item{lambda}{Regularization parameter controlling the SIC-type penalty.}

\item{epsilon}{Small positive constant used for numerical stabilization
in the penalty term.}
}
\value{
A numeric vector containing the concatenated gradients with respect
to \code{B} and \code{B_zero}.
}
\description{
Computes the gradient of the negative penalized ELBO with respect to the
model parameters in the M-step of a multivariate zero-inflated Poisson
log-normal model.
}
\details{
The gradient is evaluated with respect to the regression coefficients
\code{B} (latent Gaussian component) and \code{B_zero} (zero-inflation
component), while variational parameters are held fixed.

The gradient consists of two blocks:
\itemize{
\item Gradient with respect to \code{B}, including the Gaussian likelihood
contribution and the SIC-type sparsity penalty,
\item Gradient with respect to \code{B_zero}, corresponding to the
logistic zero-inflation model.
}

The SIC penalty is a smooth sparsity-inducing penalty defined as:
\deqn{
\frac{\lambda}{2} \frac{2 B \epsilon^2}{(B^2 + \epsilon^2)^2}.
}

The function returns a vectorized gradient suitable for optimization
routines such as \code{optim}.
}
\note{
The gradient assumes that variational parameters (\code{M}, \code{S}, \code{R})
are fixed during the M-step optimization.
}
\examples{
# Compute gradient at current M-step parameters
# g <- grad_M_step(
#   X, Y, O,
#   paramsM,
#   Omega,
#   M, S, R,
#   X_zero,
#   lambda = 1,
#   epsilon = 1e-4
# )

}

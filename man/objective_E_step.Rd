% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Uselful_functions.R
\name{objective_E_step}
\alias{objective_E_step}
\title{Objective Function for the Variational E-Step}
\usage{
objective_E_step(
  X,
  Y,
  O = O,
  params,
  B,
  Omega,
  X_zero,
  B_zero,
  lambda,
  epsilon
)
}
\arguments{
\item{X}{Numeric matrix of covariates for the latent Gaussian component
(\eqn{n \times d}).}

\item{Y}{Matrix of observed counts (\eqn{n \times p}).}

\item{O}{Matrix of offsets on the log scale (\eqn{n \times p}).}

\item{params}{Vector containing the variational parameters stacked in the
following order: means (\code{M}), standard deviations (\code{S}), and
posterior probabilities of structural zeros (\code{R}).}

\item{B}{Matrix of regression coefficients for the latent Gaussian model.}

\item{Omega}{Precision matrix of the latent Gaussian distribution
(\eqn{\Omega = \Sigma^{-1}}).}

\item{X_zero}{Design matrix for the zero-inflation component.}

\item{B_zero}{Matrix of regression coefficients for the zero-inflation model.}

\item{lambda}{Non-negative regularization parameter controlling the SIC
penalty.}

\item{epsilon}{Small positive constant used in the SIC to
avoid numerical instability.}
}
\value{
A numeric value corresponding to the negative penalized ELBO.
The value is negated so that standard minimization algorithms can be used.
}
\description{
Computes the negative penalized evidence lower bound (ELBO) used during
the variational E-step of a multivariate zero-inflated Poisson log-normal
model. The function is intended to be minimized with respect to the
variational parameters.
}
\details{
The ELBO consists of:
\itemize{
\item the expected log-likelihood of the Poisson component,
\item the expected log-likelihood of the zero-inflation component,
\item the entropy of the latent Bernoulli indicators,
\item the variational Gaussian contribution.
}

A smooth SIC-type penalty is added on the regression coefficients:
\deqn{
\frac{\lambda}{2}
\sum_j \frac{B_j^2}{B_j^2 + \epsilon^2},
}
promoting sparse solutions while maintaining differentiability.

The function returns the negative penalized ELBO because most optimization
routines in R perform minimization rather than maximization.
}
\examples{
# Negative ELBO at current variational parameters
# objective_E_step(
#   X = X,
#   Y = Y,
#   O = O,
#   params = params,
#   B = B,
#   Omega = Omega,
#   X_zero = X_zero,
#   B_zero = B_zero,
#   lambda = 1,
#   epsilon = 1e-4
# )

}

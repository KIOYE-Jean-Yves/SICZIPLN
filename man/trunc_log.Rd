% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Uselful_functions.R
\name{trunc_log}
\alias{trunc_log}
\title{Compute a numerically stable logarithm}
\usage{
trunc_log(tens, eps = 1e-16)
}
\arguments{
\item{tens}{A numeric scalar, vector, matrix, or array.}

\item{eps}{A small positive constant used to bound the values before
applying the logarithm. Default is \code{1e-16}.}
}
\value{
An object with the same dimensions as \code{tens}, containing the
logarithm of the truncated values.
}
\description{
Computes the natural logarithm after truncating the input values
to the interval \link[=`eps`, `1 - eps`]{\code{eps}, \code{1 - eps}}. This prevents numerical issues
caused by values equal or very close to 0 or 1.
}
\details{
Each element of \code{tens} is first truncated to the interval
\link[=`eps`, `1 - eps`]{\code{eps}, \code{1 - eps}} using:
\deqn{\max(\min(x, 1-\varepsilon), \varepsilon)}
and the natural logarithm is then applied.

This function is useful when the probabilities are close to 0 or 1 may
lead to numerical instability log(0)= -Inf
}
\examples{
trunc_log(c(0, 1e-20, 0.2, 0.8, 1))

}

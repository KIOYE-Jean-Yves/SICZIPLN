% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Uselful_functions.R
\name{objective_M_step_a_supprimer}
\alias{objective_M_step_a_supprimer}
\title{Objective Function for the Variational M-Step}
\usage{
objective_M_step_a_supprimer(
  X,
  Y,
  O = O,
  paramsM,
  Omega,
  M,
  S,
  R,
  X_zero,
  lambda,
  epsilon
)
}
\arguments{
\item{X}{Numeric matrix of covariates for the latent Gaussian component
(\eqn{n \times d}).}

\item{Y}{Matrix of observed count data (\eqn{n \times p}).}

\item{O}{Matrix of offsets on the log scale (\eqn{n \times p}).}

\item{paramsM}{Numeric vector containing model parameters stacked as:
regression coefficients for the latent Gaussian component (\code{B})
followed by coefficients for the zero-inflation model (\code{B_zero}).}

\item{Omega}{Precision matrix of the latent Gaussian distribution
(\eqn{\Omega = \Sigma^{-1}}).}

\item{M}{Matrix of variational means (\eqn{n \times p}).}

\item{S}{Matrix of variational standard deviations (\eqn{n \times p}).}

\item{R}{Matrix of posterior probabilities of structural zeros (\eqn{n \times p}).}

\item{X_zero}{Design matrix for the zero-inflation component.}

\item{lambda}{Regularization parameter controlling the SIC-type penalty.}

\item{epsilon}{Small positive constant used for numerical stabilization
in the penalty term.}
}
\value{
A numeric value corresponding to the negative penalized ELBO.
The function is designed for minimization in numerical optimization
procedures.
}
\description{
Computes the negative penalized evidence lower bound (ELBO) used in the
variational M-step of a multivariate zero-inflated Poisson log-normal model.
The function is optimized with respect to the model parameters
\code{B} and \code{B_zero}, while variational parameters are held fixed.
}
\details{
The objective function is composed of:
\itemize{
\item Expected Poisson log-likelihood under the variational distribution,
\item Zero-inflation logistic likelihood,
\item Entropy of latent Bernoulli variables,
\item Gaussian variational contribution,
\item SIC-type sparsity penalty on regression coefficients.
}

The penalty term encourages sparsity in \code{B} while remaining smooth
for gradient-based optimization.
}
\note{
The function returns the negative ELBO to be compatible with standard
minimization routines such as \code{optim}.
}
\examples{
# Evaluate objective at current parameters
# obj <- objective_M_step(
#   X, Y, O,
#   paramsM,
#   Omega,
#   M, S, R,
#   X_zero,
#   lambda = 1,
#   epsilon = 1e-4
# )

}
